<%= ui_page(max_width: :md) do %>
  <%= ui_page_header(title: "Settings") %>

  <% @areas.each do |area, sections| %>
    <section aria-label="<%= area.to_s.titleize %>">
      <%= ui_section(title: area.to_s.titleize) do %>
        <%= ui_panel do %>
          <nav aria-label="Settings">
            <% sections.each do |section| %>
              <%= link_to section.title, section_path(section.key) %>
            <% end %>
          </nav>
        <% end %>
      <% end %>
    </section>
  <% end %>

  <%= render partial: @section.renders if @section %>
<% end %>
